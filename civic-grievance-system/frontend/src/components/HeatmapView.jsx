import { MapContainer, TileLayer, CircleMarker, Popup } from 'react-leaflet'; import 'leaflet/dist/leaflet.css';
export default function HeatmapView({ complaints = [] }) { const center = complaints[0] ? [complaints[0].lat, complaints[0].lng] : [20.5937, 78.9629]; return <MapContainer center={center} zoom={5} className="map"><TileLayer attribution="&copy; OpenStreetMap" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" />{complaints.map((item) => <CircleMarker key={item.id} center={[item.lat, item.lng]} pathOptions={{ color: item.status === 'Marked Fixed' ? '#27856a' : item.severity === 'High' ? '#d34d3f' : '#d49b32' }} radius={Math.min(18, 6 + item.affected_citizens)}><Popup>#{item.id} · {item.category}<br />{item.status}</Popup></CircleMarker>)}</MapContainer>; }